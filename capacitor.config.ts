import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.golgelabirenti.game',
  appName: 'Gölge Labirenti',
  webDir: 'dist',
  backgroundColor: '#000000'
};

export default config;
